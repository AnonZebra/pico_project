<!DOCTYPE html>
<html class="no-js">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="author" content="{{ meta.author }}">
    <link rel="icon" href="{{ theme_url }}/../shared/img/favicon.ico">
    <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>

    {% if meta.description %}
        <meta name="description" content="{{ meta.description|striptags }}" />
    {% endif %}
    {% if meta.robots %}
        <meta name="robots" content="{{ meta.robots }}" />
    {% endif %}

    {% if current_page %}
        <link rel="canonical" href="{{ current_page.url }}" />
    {% endif %}

    <link rel="stylesheet" href="{{ theme_url }}/css/normalize.min.css" type="text/css" />
    <link rel="stylesheet" href="{{ theme_url }}/css/style.css" type="text/css" />
</head>
<body{% if config.theme_config.widescreen %} class="widescreen"{% endif %}>
    <div class="grid-root {{ meta.wrapclass }}-root">
        <header class="site-header">
            {% if pages["_meta"].meta.logo %}
                <div class="logo" aria-hidden="true">
                    <a href="{{ "index"|link }}">
                        <img src="{{ base_url }}/{{ pages["_meta"].meta.logo|url }}" alt="Site logo" />
                    </a>
                </div>
            {% endif %}
            <a id="nav-toggle" class="nav-toggle" title="Toggle Menu" role="button" aria-controls="nav" aria-expanded="false" tabindex="1">
                <i class="fa fa-bars" aria-hidden="true" id="toggler"></i>
                <span class="sr-only">Toggle Menu</span>
            </a>
            <nav id="nav" role="navigation" tabindex="-1">
                <ul>
                    {% for page in pages(depthOffset=-1) if page.title and not page.hidden %}
                    <li{% if page.id == current_page.id %} class="active" {% endif %}>
                        <a href="{{ page.url }}">{{ page.title }}</a>
                    </li>
                    {% endfor %}
                </ul>
            </nav>
        </header>
        <main class="grid-main{% if meta.wrapclass %} grid-main-{{ meta.wrapclass}}{% endif %}">
            <div class="content-wrapper">
                {{ content }}
            </div>
            <ul class="report-box-list">
                {% for child in current_page.tree_node.children %}
                    {% if child.page.title != "Slutprojekt" %}
                        <li class="report-box-li">
                            <a href="{{ child.page.url }}">
                                <div class="report-box hexagon double">
                                    <div class="report-box-inner">
                                        <h2 class="report-box-title"> {{ child.page.title }}</h2>
                                        <p class="report-box-intro"> {{ child.page.meta.intro }}</p>
                                    </div>
                                </div>
                            </a>
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>
            <div class="report-project">
                <a href="{{ attribute(pages, "report/kmom10").url }}">
                    <div class="report-box hexagon double">
                        <div class="report-box-inner">
                            <h2 class="report-box-title">{{ attribute(pages, "report/kmom10").title }}</h2>
                            <p class="report-box-intro">{{ attribute(pages, "report/kmom10").meta.intro }}</p>
                            <img src="{{ base_url }}/{{ pages["report/kmom10"].meta.image|url }}" alt="{{ pages["report/kmom10"].meta.imagedesc }}" />
                        </div>
                    </div>
                </a>
            </div>


        </main>
        <footer class="site-footer">
            <p><q>
                Our responsibility is to do what we can, learn what we can, improve the solutions, and pass them on.
            </q>
            - Richard P. Feynman
            </p>
            <div class="container">
                <div class="social">
                    <!-- Loads from FontAwesome (themes/shared/css/font-awesome.min.css) -->
                    {% for social in pages["_meta"].meta.social %}
                        <a href="{{ social.url }}" title="{{ social.title }}" class="icon-link">
                            <i class="{{ social.icon }} logo" aria-hidden="true"></i>
                            <span class="sr-only">{{ social.title }}</span>
                        </a>
                    {% endfor %}
                </div>
            </div>
        </footer>
    </div>
    <script src="{{ theme_url }}/../shared/js/modernizr-3.3.1-custom.min.js" type="text/javascript"></script>
    <script src="{{ theme_url }}/../shared/js/utils.js" type="text/javascript"></script>
    <script src="{{ theme_url }}/../shared/js/responsive-navbar.js" type="text/javascript"></script>
</body>
</html>
